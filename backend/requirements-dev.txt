# Dev/test-only dependencies (not needed at runtime).
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.25
httpx>=0.27,<0.28          # FastAPI/Starlette TestClient transport
