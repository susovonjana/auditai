# Loaded automatically by `npm run dev`.
# Points the vite dev-server proxy at the local backend by default.
# Override per-developer in .env.development.local (gitignored).
# VITE_API_TARGET=http://localhost:8000
VITE_API_TARGET=http://1audit-dev-ec2-alb-253123334.eu-west-1.elb.amazonaws.com:8000
